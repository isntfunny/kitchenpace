'use client';

// TODO: Fix ChatwootScript integration — needs proper initialization
// import { ChatwootScript } from 'chatwoot-react';

export function ChatwootWidgetComponent() {
    // Chatwoot widget disabled pending proper setup
    return null;
}
